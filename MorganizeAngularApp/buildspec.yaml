version: 0.2

phases:
    install:
      runtime-versions:
        nodejs: 10
      commands:
      - npm install -g @angular/cli
      - npm install --save-dev @angular-devkit/build-angular
    build:
      commands:
        - cd MorganizeAngularApp
        - ng build --prod
    post_build:
      commands:
        - aws s3 cp dist/MorganizeAngularApp s3://morganize.com --recursive